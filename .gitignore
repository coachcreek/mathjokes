# Node.js dependencies installed by npm
node_modules/

# dist/ is intentionally NOT ignored — it must be committed so Cloudflare
# deploys the minified files alongside index.html and the CSS.
# After any JS change, run: npm run build   then commit the updated dist/js/ files.


# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example