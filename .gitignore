# Node.js dependencies installed by npm
node_modules/

# Minified build output (generated by `npm run build`)
# These files are reproduced from source — no need to version-control them.
# To regenerate: npm install && npm run build
dist/


# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example