{
  "name": "mathjokes",
  "version": "1.0.0",
  "description": "Math Jokes Worksheet Generator — build scripts",
  "private": true,
  "scripts": {
    "build": "node build.js"
  },
  "devDependencies": {
    "html-minifier-terser": "^7.2.0",
    "terser": "^5.0.0"
  }
}
